[tool.poetry]
name = "colocationpy"
version = "0.1.0"
description = "A pacakge to identify instances of co-location between individuals within a population."
authors = ["Keiran Suchak <ksuchak1990@yahoo.co.uk>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
scikit-mobility = "^1.3.1"
numpy = "^2.1.1"
shapely = "^2.0.6"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.3"
black = "^24.8.0"
pytest-cov = "^5.0.0"
neovim = "^0.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
